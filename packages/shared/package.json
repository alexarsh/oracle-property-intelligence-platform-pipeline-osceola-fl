{
  "name": "@osceola/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared contracts for the Osceola pipeline: run manifest, run history, query-table schemas, county configuration (zod + TypeScript).",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "scripts": { "build": "tsc -b", "clean": "rm -rf dist" },
  "dependencies": { "zod": "^3.25.0" }
}
